server:
  port: 7777


# 配置activemq
spring:
  activemq:
    # activemq服务器地址
    broker-url: tcp://localhost:61616
    # 用户名
    user: admin
    # 密码
    password: admin
  # false: Queue(默认值)
  # true: Topic
  jms:
    pub-sub-domain: false


# 定义队列名称
myqueue: boot-activemq-queue